<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Lab 4</title>
    <link rel="stylesheet" type="text/css" href="~/css/StyleSheet.css" />
</head>
<body>
    <header>
        <h1>Welcome to Lab 4</h1>
        <nav>
            @Html.ActionLink("Home", "Index", "Home")
            @Html.ActionLink("Show the song!", "SongForm", "Home")
            @Html.ActionLink("Students", "CreateStudent", "Home")
        </nav>
    </header>
    <div class="container">
        @RenderBody()
    </div>
    <footer>
        <p>
            Layout description and reason for CSS and layout locations.
            <br />
            CSS is in the `wwwroot/css` directory because it is the standard location for static files.
            <br />
            Layout is in the `Views/Shared` directory because it can be shared across multiple views.
        </p>
    </footer>
    @RenderSection("Scripts", required: false)
</body>
</html>

